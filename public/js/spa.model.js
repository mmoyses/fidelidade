spa.model = (function() {
  'use strict';
  var configMap = {
  },
  stateMap = {
  },
  isFakeData = true,
  initModule;

  initModule = function() {};

  return {
    initModule: initModule,
  };

}());
